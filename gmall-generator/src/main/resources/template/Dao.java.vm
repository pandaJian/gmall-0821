package ${package}.${moduleName}.com.atguigu.gmall.scheduled.mapper;

import ${package}.${moduleName}.entity.${className}Entity;
import com.baomidou.mybatisplus.core.com.atguigu.gmall.scheduled.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Mapper extends BaseMapper<${className}Entity> {
	
}
